class Graph(object):
    def __init__(self):
        self.vertices=set([])
        self.edges=set([])

    def addV(self, (i,j)):
        self.vertices.add((i,j))

    def addEgge(self, v1, v2):
        self.edges.add(set([v1, v2])

    def checkRight(a, (i,j)):
        x,y = (i,j+1)
        if y > numCols:
           return None
        return(a[x][y]==1, (x,y))


    class Graph


    def check_left    #check left should be x,y = (i, j-1) and check edges
                          # with y < 0
       
    def createGraph(a):
        graph = Graph()

        for i in rows:
            for j in cols:
                if a[i][j] == 1:
                       graph.add((i,j))
        returns graph

    def createGraph(a):
                       graph = Graph()
                       for i in rows:
                           for j in cols:
                               if a[1][j] == 1:
                                   graph.add((i,j))
                                   check right (a, (i,j))
                                   check left
                                   check down
                                   check up
                       return graph



    class Graph:

                       def checkRight
                       def creategraph


    call the thing: g = createGraph(a)
                       i = countIslands.
    
